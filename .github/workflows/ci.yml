name: Updates Repo

on:
  push:
    branches:
      - test
  schedule:
    - cron: "0/5 * * * *"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Handle Repo1
      uses: actions/checkout@v1.0.0
      with:
        ref: refs/heads/test
    - name: Test Run
      uses: ./
      